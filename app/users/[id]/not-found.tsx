import React from "react";

// We can also create not found pages for end points to show custom messages

const UserNotFoundPage = () => {
  return <div>This User does not exist.</div>;
};

export default UserNotFoundPage;
